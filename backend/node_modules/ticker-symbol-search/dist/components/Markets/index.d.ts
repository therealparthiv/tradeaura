/// <reference types="react" />
import { MarketTypes } from "../../types/markets";
declare const Markets: (props: {
    markets: string[];
    selectedMarket: string;
    updateMarket: (newMarket: MarketTypes) => void;
}) => JSX.Element;
export default Markets;
